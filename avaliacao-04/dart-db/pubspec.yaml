name: dart_db
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  sqlite3: ^2.0.0  # Check for the latest version on pub.dev

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
